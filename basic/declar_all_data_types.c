#include <stdio.h>

int main() {

	short short_integer; // normamente >= 16 bits
	int integer; // normalmente >= short(16 bits)
	long long_integer; // normalmente >= int(32 bits)

	float float_basic; // normalmente == 32 bits IEEE float 
	double float_double; // normamente == 64 bits IEEE float

	char unsigned_data_type; // 1,2,3,4,5,6,7,8,9... bits
	char line[1000]; // string

}
